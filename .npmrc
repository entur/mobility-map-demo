# Security configurations for npm
# Disable pre/post install scripts to prevent malicious code execution
ignore-scripts=true

# Set minimum audit level for security vulnerabilities
audit-level=moderate

# Enable strict SSL for registry connections
strict-ssl=true

# Save exact versions by default
save-exact=true

# Prevent npm from modifying package-lock.json unnecessarily
package-lock=true